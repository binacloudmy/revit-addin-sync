<Window x:Class="RevitWebAppSync.UI.SimpleProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing..."
        Height="180"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Topmost="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Name="TitleText"
                   Text="Processing..."
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#333333"
                   HorizontalAlignment="Center"/>

        <!-- Status Text -->
        <TextBlock Grid.Row="2"
                   Name="StatusText"
                   Text="Please wait..."
                   FontSize="13"
                   Foreground="#666666"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   TextAlignment="Center"/>

        <!-- Progress Bar (Indeterminate) -->
        <ProgressBar Grid.Row="4"
                     Name="ProgressBar"
                     Height="20"
                     IsIndeterminate="True"
                     Background="#E0E0E0"
                     Foreground="#2196F3"/>
    </Grid>
</Window>
