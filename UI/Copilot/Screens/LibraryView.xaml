<UserControl x:Class="RevitWebAppSync.UI.Copilot.Screens.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctl="clr-namespace:RevitWebAppSync.UI.Copilot.Controls"
             FontFamily="{DynamicResource Cp.Font}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search -->
        <Border Grid.Row="0" Padding="14,10,14,8">
            <Border BorderBrush="{DynamicResource Cp.Line}" BorderThickness="1" CornerRadius="10"
                    Background="{DynamicResource Cp.LineSoft}" Padding="12,8">
                <DockPanel LastChildFill="True">
                    <Path DockPanel.Dock="Left" Width="14" Height="14" Stretch="Uniform" Stroke="{DynamicResource Cp.Muted}"
                          StrokeThickness="1.6" VerticalAlignment="Center" Margin="0,0,8,0"
                          Data="M4,11 A7,7 0 1 0 18,11 A7,7 0 1 0 4,11 Z M21,21 l-4.3,-4.3"/>
                    <TextBox Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0"
                             Background="Transparent" FontSize="13" VerticalAlignment="Center"
                             Foreground="{DynamicResource Cp.Ink}"/>
                </DockPanel>
            </Border>
        </Border>

        <!-- Category chips (built in code-behind for active-state) -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Padding="14,0,14,10">
            <StackPanel x:Name="ChipsHost" Orientation="Horizontal"/>
        </ScrollViewer>

        <!-- List: recent + vetted + ai + fallback -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="14,0,14,14">
            <StackPanel x:Name="ListHost"/>
        </ScrollViewer>

        <ctl:PromptBar Grid.Row="3" Placeholder="Quick ask Copilot…" SubmitCommand="{Binding ChatSendCommand}" IsBusy="{Binding IsRouting}"/>
    </Grid>
</UserControl>
