<Window x:Class="RevitWebAppSync.IssuePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BINA Issues"
        Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Issues from BINA"
                   FontSize="20" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1" x:Name="SubtitleText"
                   Foreground="#666666" Margin="0,0,0,12" TextWrapping="Wrap"/>

        <ListView Grid.Row="2" x:Name="IssueList"
                  BorderBrush="#CCCCCC" BorderThickness="1" Background="White"
                  MouseDoubleClick="IssueList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Issue" Width="300" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Status" Width="90" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Priority" Width="80" DisplayMemberBinding="{Binding Priority}"/>
                    <GridViewColumn Header="Model" Width="150" DisplayMemberBinding="{Binding DesignName}"/>
                    <GridViewColumn Header="Raised by" Width="120" DisplayMemberBinding="{Binding Author.Name}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Grid.Row="3" x:Name="StatusText"
                   Foreground="#666666" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="CloseButton" Content="Close" Width="100" Height="32"
                    Margin="0,0,10,0" Click="CloseButton_Click" IsCancel="True"/>
            <Button x:Name="ShowButton" Content="Show in model" Width="150" Height="32"
                    Click="ShowButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
