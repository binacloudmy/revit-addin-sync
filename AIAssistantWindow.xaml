<Window x:Class="RevitWebAppSync.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit AI Assistant"
        Width="520" Height="650"
        MinWidth="420" MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <!-- Dark theme styles -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1084D9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Tab header style -->
        <Style x:Key="DarkTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                Cursor="Hand">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="TabBorder" Property="Background" Value="#3F3F46"/>
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Revit AI Assistant"
                       FontSize="20" FontWeight="Bold"
                       Foreground="#FFFFFF"/>
            <TextBlock x:Name="StatusText"
                       Text="Ready"
                       FontSize="12"
                       Foreground="#888888"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tabbed Chat Areas -->
        <TabControl x:Name="ModeTabs"
                    Grid.Row="1"
                    Background="Transparent"
                    BorderThickness="0"
                    Margin="0,0,0,12"
                    SelectionChanged="ModeTabs_SelectionChanged">

            <!-- Revit Copilot Tab -->
            <TabItem Header="🤖 Revit Copilot"
                     Style="{StaticResource DarkTabItem}">
                <Border Background="#252526" CornerRadius="0,8,8,8">
                    <ScrollViewer x:Name="CopilotScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="12">
                        <StackPanel x:Name="CopilotChatHistory"/>
                    </ScrollViewer>
                </Border>
            </TabItem>

            <!-- JKR Guide Tab -->
            <TabItem Header="📖 JKR Guide"
                     Style="{StaticResource DarkTabItem}">
                <Border Background="#252526" CornerRadius="0,8,8,8">
                    <ScrollViewer x:Name="JkrScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="12">
                        <StackPanel x:Name="JkrChatHistory"/>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Input Area -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PromptInput"
                     Grid.Column="0"
                     Style="{StaticResource DarkTextBox}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MaxHeight="100"
                     VerticalScrollBarVisibility="Auto"
                     KeyDown="PromptInput_KeyDown"
                     Text=""
                     ToolTip="Enter your prompt (Ctrl+Enter to send)"/>

            <Button x:Name="SendButton"
                    Grid.Column="1"
                    Content="Send"
                    Style="{StaticResource DarkButton}"
                    Margin="8,0,0,0"
                    Click="SendButton_Click"/>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock Text="Powered by BINA AI"
                       FontSize="10"
                       Foreground="#666666"/>
            <TextBlock x:Name="TokensText"
                       Text=""
                       FontSize="10"
                       Foreground="#666666"
                       Margin="16,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
