<Window x:Class="RevitWebAppSync.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Download BIM Disciplines"
        Height="450"
        Width="500"
        MinHeight="350"
        MinWidth="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   Text="Downloading..."
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#1976D2"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Summary -->
        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Text="Starting download..."
                   FontSize="12"
                   Foreground="#666"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <!-- Progress Bar -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <ProgressBar x:Name="DownloadProgressBar"
                         Height="8"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Background="#E0E0E0"
                         Foreground="#4CAF50"/>
        </Grid>

        <!-- File List -->
        <Border Grid.Row="3"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="FileListControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Status Tag -->
                                <Border Grid.Column="0"
                                        Grid.RowSpan="2"
                                        Background="{Binding StatusBackground}"
                                        CornerRadius="3"
                                        Padding="6,2"
                                        Margin="0,0,10,0"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusTag}"
                                               FontSize="10"
                                               FontWeight="Bold"
                                               Foreground="White"/>
                                </Border>

                                <!-- Display Name (Discipline / Folder) -->
                                <TextBlock Grid.Column="1"
                                           Grid.Row="0"
                                           Text="{Binding DisplayName}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="#333"/>

                                <!-- File Path -->
                                <TextBlock Grid.Column="1"
                                           Grid.Row="1"
                                           Text="{Binding FileName}"
                                           FontSize="10"
                                           Foreground="#888"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Save Location Footer -->
        <StackPanel Grid.Row="4" Margin="0,15,0,0">
            <TextBlock x:Name="SaveLocationText"
                       Text=""
                       FontSize="11"
                       Foreground="#666"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <!-- Close Button -->
        <Button x:Name="CloseButton"
                Grid.Row="5"
                Content="Close"
                Width="100"
                Height="35"
                Margin="0,15,0,0"
                HorizontalAlignment="Center"
                Background="#424242"
                Foreground="White"
                BorderThickness="0"
                IsEnabled="False"
                Click="CloseButton_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
